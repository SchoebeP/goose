.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.ipa
*.dSYM
*.xcarchive
Rust/include/*.tmp
Rust/include/
Rust/libgoose_core.a
Rust/libgoose_core.a.tmp
Rust/libcodex_ios_auth_bridge.a
Rust/.goose_core.active-target
Rust/iphoneos/
Rust/iphonesimulator/
Vendor/CodexRust/codex-rs/target/
tmp/

# local-only
.env
.idea/
build-device/
build-release/
.superpowers/
imu.png

build-sim/
build-device/
build-release/
build/

# --- secrets (NEVER commit; this repo is PUBLIC) ---
**/.env
*.token
ingest-token.txt

# --- real biometric / captured data: stays on the owner's machines only ---
# Root enabler of the goose-raw-export leak — keep these broad.
*.sqlite
*.sqlite3
*.db
*.csv
*.jsonl
goose-raw-export-*/
**/goose-raw-export-*/
# ...but legitimate, non-personal decode fixtures may live here:
!Rust/core/fixtures/**
